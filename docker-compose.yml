version: '3.8'

services:

  backend:
    image: document-approval-backend
    build: .
    container_name: backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
      SPRING_DATASOURCE_USERNAME: postgres.ebbcfvlheajlzacroprm
      SPRING_DATASOURCE_PASSWORD: Harsha@143123
    ports:
      - "8080:8080"
    networks:
      - app-network

  frontend:
    image: document-approval-frontend
    build: ./frontend
    container_name: frontend
    ports:
      - "3000:80"
    networks:
      - app-network
    depends_on:
      - backend

networks:
  app-network:
    driver: bridge
